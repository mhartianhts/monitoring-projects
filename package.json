{
  "name": "local-project-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Local Project Manager Dashboard",
  "scripts": {
    "dev": "concurrently -n be,fe -c teal,magenta \"npm run dev:be\" \"npm run dev:fe\"",
    "dev:all": "concurrently -n be,fe,converter -c teal,magenta,yellow \"npm run dev:be\" \"npm run dev:fe\" \"npm run dev:converter\"",
    "dev:be": "npm run dev --prefix backend",
    "dev:fe": "npm run dev --prefix frontend",
    "dev:converter": "cd services/converter-service && .\\venv\\Scripts\\python.exe src/server.py",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
